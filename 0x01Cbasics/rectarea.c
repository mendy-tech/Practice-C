#include <stdio.h>

/*
 * main - calculates the area of a rectangle.
 * Return: Always 0.
 */

int main(void)
{
	int l;
	int w;
	l = 8;

	w = 4;
	printf("The area of a rectangle is %d\n", l * w);
	return (0);
}
