#include <stdio.h>

/*
 * main - prints area of rectangle using inputs from the user.
 * Return: Always 0.
 */

int main(void)
{
	int l;
	int w;
	printf("Area of a rectangle is %d\n", l * w);

	scanf("length is: %d\n"," width is:%d\n", &l, &w);
	return (0);
}
